<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="assets/css/theme.css" rel="stylesheet" />
    <style>
        .foundResult{
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 14px 28px rgba(0, 0, 0, 0.25), 0 10px 10px rgba(0, 0, 0, 0.22);
            position: absolute;
            left: 25%;
            top: 10%;
            overflow: hidden;
            /* width: 678px; */
            width: 750px;
            max-width: 100%;
            height: 50%;
            padding: 40px;
        }
        label{
            font-size: 2rem;
            margin-right: 3rem;
            width: 15rem;
        }
        select{
            width: 50%;
        }

        h1{
            margin-bottom: 4%;
        }
        .btn{
            margin-top: 7%;
        }
    </style>

</head>
<body>
<main>

    <div style="height: 45rem;">
        
        <div class="foundResult">

            <h1>Enter the shipment details</h1>

            <form action="/newCourier" method="post">

                <label for="">Source</label> 

                <select name="source" >
                    <option value="Select">Select</option>
                    <% stops.forEach((obj)=>{ %>
                        <option value=<%=obj.pincode %> > <%=obj.name %> </option>
                    <%}) %>
                </select> 

                <label for="">Destination</label>

                <select name="destination" >
                    <option value="Select">Select</option>
                    <% stops.forEach((obj)=>{ %>
                        <option value=<%=obj.pincode %> > <%=obj.name %> </option>
                    <%}) %>
                </select>

                <input class="btn btn-primary me-2" type="submit">

            </form>

        </div>

    </div>

</main>
</body>
</html>